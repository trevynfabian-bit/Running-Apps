{
  "name": "@running/health-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Native HealthKit bridge (iOS only) with an Expo config plugin.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./app.plugin.js": "./plugin/withHealthKit.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "expo": "*",
    "react-native": "*"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.10"
  }
}
