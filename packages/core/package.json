{
  "name": "@running/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure domain model and performance engines. Zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^4.1.10"
  }
}
